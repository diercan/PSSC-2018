@model IEnumerable<UPT.Models.Curs>

@{
    ViewBag.Title = "Cursuri";
}

<h2>Cursuri</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDCurs)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Titlu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Credite)
        </th>
        <th>
            Sectie
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDCurs)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Titlu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Credite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Departament.Nume)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCurs }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCurs }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCurs })
        </td>
    </tr>
}

</table>
